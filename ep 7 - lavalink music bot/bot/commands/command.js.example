const discord = require('discord.js');

module.exports = {
    /**
     * 
     * @param {string[]} args 
     * @param {discord.Message} message 
     */
    run: (args, message) => {

    },

    command: 'yourcommandhere'
}